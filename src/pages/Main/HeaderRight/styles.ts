import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  headerButton: {
    padding: 3,
    marginRight: 5,
    borderRadius: 50,
  },
});

export default styles;
