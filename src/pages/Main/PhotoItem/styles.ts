import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    elevation: 1,
    borderWidth: 1,
    borderColor: '#bbb',
    backgroundColor: '#fff',
    borderRadius: 5,
    padding: 10,
  },
  image: {
    width: 150,
    height: 150,
  },
  title: {
    fontSize: 16,
  },
});

export default styles;
