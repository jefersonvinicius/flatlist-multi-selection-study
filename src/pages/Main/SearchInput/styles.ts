import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  input: {
    elevation: 1,
    backgroundColor: '#fff',
    borderRadius: 5,
  },
});

export default styles;
