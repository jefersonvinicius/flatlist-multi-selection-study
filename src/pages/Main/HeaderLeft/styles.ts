import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  headerButton: {
    padding: 3,
    marginRight: 5,
    borderRadius: 50,
  },
  title: {
    fontSize: 20,
    color: '#fff',
    fontWeight: '600',
  },
});

export default styles;
